---
export interface Props {
  type?: 'tip' | 'note' | 'warning' | 'pro' | 'info';
}

const { type = 'info' } = Astro.props;

// Define styles for different callout types
const calloutStyles = {
  tip: {
    container: 'my-6 rounded-lg border-l-4 border p-4 bg-blue-950 border-blue-800',
    icon: '💡',
    title: 'Tip',
    textColor: 'flex-1 text-blue-100'
  },
  note: {
    container: 'my-6 rounded-lg border-l-4 border p-4 bg-gray-900 border-gray-700',
    icon: '📝',
    title: 'Note',
    textColor: 'flex-1 text-gray-100'
  },
  warning: {
    container: 'my-6 rounded-lg border-l-4 border p-4 bg-yellow-950 border-yellow-800',
    icon: '⚠️',
    title: 'Warning',
    textColor: 'flex-1 text-yellow-100'
  },
  pro: {
    container: 'my-6 rounded-lg border-l-4 border p-4 bg-purple-950 border-purple-800',
    icon: '⚡',
    title: 'Pro Tip',
    textColor: 'flex-1 text-purple-100'
  },
  info: {
    container: 'my-6 rounded-lg border-l-4 border p-4 bg-indigo-950 border-indigo-800',
    icon: 'ℹ️',
    title: 'Info',
    textColor: 'flex-1 text-indigo-100'
  }
};

const style = calloutStyles[type];
---
<div class={style.container}>
  <div class="flex items-start">
    <div class="flex-shrink-0 mr-3">
      <span class="text-lg" role="img" aria-label={`${style.title} icon`}>
        {style.icon}
      </span>
    </div>
    <div class={style.textColor}>
      <div class="prose prose-sm max-w-none dark:prose-invert">
        <slot />
      </div>
    </div>
  </div>
</div>
