---
export interface Props {
  slug: string;
}

const { slug } = Astro.props;

const image = new URL(`./open-graph/posts/${slug}.png`, Astro.url.origin).href;
const blogUrl = new URL(`./posts/${slug}`, Astro.url.origin).href;
---

<div class="flex w-full justify-center">
  <div
    class="group relative transform cursor-pointer rounded-lg p-2 shadow-lg transition duration-300 ease-in-out hover:scale-105 hover:border-2 hover:border-primary-500 hover:shadow-xl"
  >
    <a href={blogUrl} target="_blank" rel="noopener noreferrer">
      <!-- Image -->
      <img
        src={image}
        alt="Open Graph Image"
        class="rounded-t-lg object-cover"
        decoding="async"
        loading="eager"
        width="300"
        height="158"
      />
      <div
        class="absolute inset-0 flex items-center justify-center opacity-0 transition-opacity group-hover:opacity-100"
      >
        <span class="text-lg font-semibold text-white">See also</span>
      </div>
    </a>
  </div>
</div>
